# 代码检测文档

> **对应论文**：湖南大学本科毕业论文《昇腾场景下基于 vLLM-Ascend 的投机解码联合优化》
>
> **分支**：`MQA_FP` → `vllm-ascend`
>
> **vLLM 版本**：v0.9.1（`VLLM_USE_V1=0`）
>
> **更新日期**：2026-04-20

---

## 1. 代码检测范围总览

本文档按照论文第四章实验结构，将核心代码改动分为以下四个模块：

| 模块 | 代码文件 | 所属层级 |
|------|----------|----------|
| FastPath 优化 | `vllm-ascend/vllm_ascend/worker/draft_model_runner.py` | Draft Model Runner |
| MQA 打分路径 | `vllm/vllm/spec_decode/mqa_scorer.py` | Speculative Scorer |
| Logits 对齐校准 | `vllm-ascend/vllm_ascend/worker/draft_model_runner.py` | Draft Model Runner |
| 动态步长控制器 | `vllm/vllm/spec_decode/spec_decode_worker.py` | Spec Decode Worker |

> **注意**：本文档所列代码行号以各文件实际内容为准，如有出入请以源文件为准。

---

## 2. FastPath（DraftGPUFastPath）代码实现

### 2.1 概述

FastPath（草案 GPU 快速路径）是昇腾 NPU 场景下的一项多步执行优化。传统投机解码在每个草案 token 生成步骤后都需要将采样结果从 GPU 回传 CPU 进行调度决策，FastPath 通过在 GPU 侧直接推进 decode 迭代，避免了中间 GPU→CPU→GPU 的同步开销，仅在全部 k 个草案 token 生成完毕后才进行一次 CPU 同步。

**适用条件**（全部满足才进入 FastPath）：

1. 仅 decode 请求，不含 prefill
2. 注意力后端支持 GPU multi-step（FLASH_ATTN 或 ASCEND）
3. 无 LORA 模块
4. 无 Prompt Adapter 模块

### 2.2 配置开关与统计变量初始化

**文件**：`vllm-ascend/vllm_ascend/worker/draft_model_runner.py`
**位置**：`TP1DraftModelRunner.__init__`（第 70–87 行）

```python
# Draft GPU fast-path capability controls and reason breakdown stats.
self._gpu_multistep_backend_allowlist = self._read_backend_allowlist_env(
    "VLLM_ASCEND_SPEC_GPU_MULTI_STEP_BACKENDS", "FLASH_ATTN,ASCEND")
self.ms_total = 0
self.ms_hit = 0
self.ms_fail_disabled = 0
self.ms_fail_prompt = 0
self.ms_fail_backend = 0
self.ms_fail_lora = 0
self.ms_fail_adapter = 0
```

**说明**：
- `VLLM_ASCEND_SPEC_GPU_MULTI_STEP_BACKENDS`：允许列表环境变量，控制哪些注意力后端允许启用 FastPath，默认值为 `"FLASH_ATTN,ASCEND"`，表示 FlashAttention 和昇腾后端均可用。
- `ms_total` / `ms_hit` 等计数器用于统计 FastPath 的总体尝试次数、命中次数以及各类失败原因的细分计数。

### 2.3 注意力后端支持检测

**文件**：`vllm-ascend/vllm_ascend/worker/draft_model_runner.py`
**位置**：第 129–137 行

```python
def _attn_backend_supports_gpu_multi_step(self) -> bool:
    backend = getattr(self, "attn_backend", None)
    supports_fn = getattr(backend, "supports_spec_gpu_multi_step", None)
    if callable(supports_fn):
        try:
            return bool(supports_fn())
        except Exception as exc:
            logger.warning("supports_spec_gpu_multi_step() failed: %s", exc)
    return self._get_attn_backend_name() in self._gpu_multistep_backend_allowlist
```

**说明**：优先尝试调用后端的 `supports_spec_gpu_multi_step()` 方法，若无该方法或调用失败，则回退到白名单比对。这种双重检测机制确保了昇腾后端即使不提供显式探测函数也能通过白名单匹配进入 FastPath。

### 2.4 FastPath 命中检测与原因细分

**文件**：`vllm-ascend/vllm_ascend/worker/draft_model_runner.py`
**位置**：`supports_gpu_multi_step()` 方法，第 423–466 行

```python
def supports_gpu_multi_step(self, execute_model_req: ExecuteModelRequest):
    """Determines if draft_model_runner GPU multi-step can be used.
    Currently required conditions are:
        1. Only decodes
        2. Only flash-attn
        3. No LORA
        4. No prompt_adapter_config
    """
    self.ms_total += 1
    fail_reason = ""

    if not allow_gpu_advance_step:
        self.ms_fail_disabled += 1
        fail_reason = "disabled"

    # We allow multi-step GPU only in decode mode
    if not fail_reason:
        for seq_group in execute_model_req.seq_group_metadata_list:
            if seq_group.is_prompt:
                self.ms_fail_prompt += 1
                fail_reason = "prompt"
                break

    if (not fail_reason and
            (not self._attn_backend_supports_gpu_multi_step()
             or not self._attn_metadata_supports_advance_step())):
        self.ms_fail_backend += 1
        fail_reason = "backend"

    # TODO: Add support for LORA
    if not fail_reason and self.lora_config:
        self.ms_fail_lora += 1
        fail_reason = "lora"

    # TODO: Add soft-tuning prompt adapter support
    if not fail_reason and self.prompt_adapter_config:
        self.ms_fail_adapter += 1
        fail_reason = "adapter"

    if not fail_reason:
        self.ms_hit += 1

    self._log_gpu_multi_step_stats()
    return not fail_reason
```

**失败原因分类**：

| 计数器 | 含义 |
|--------|------|
| `ms_fail_disabled` | 全局开关 `allow_gpu_advance_step = False`（调试用途） |
| `ms_fail_prompt` | 请求包含 prefill 阶段，不满足纯 decode 条件 |
| `ms_fail_backend` | 注意力后端不支持 GPU multi-step |
| `ms_fail_lora` | 启用了 LORA 模块（当前未支持） |
| `ms_fail_adapter` | 启用了 Prompt Adapter（当前未支持） |
| `ms_hit` | 成功进入 FastPath |

### 2.5 FastPath 统计日志

**文件**：`vllm-ascend/vllm_ascend/worker/draft_model_runner.py`
**位置**：`_log_gpu_multi_step_stats()` 方法，第 150–176 行

```python
def _log_gpu_multi_step_stats(self) -> None:
    hit_rate = self.ms_hit / max(self.ms_total, 1)
    logger.info(
        "DraftGPUFastPath hit_rate=%.4f total=%d hit=%d fail_disabled=%d "
        "fail_prompt=%d fail_backend=%d fail_lora=%d fail_adapter=%d",
        hit_rate, self.ms_total, self.ms_hit, self.ms_fail_disabled,
        self.ms_fail_prompt, self.ms_fail_backend, self.ms_fail_lora,
        self.ms_fail_adapter)
```

**说明**：每次调用 `supports_gpu_multi_step()` 后都会打印当前累计统计，包括累计命中率（`hit_rate`）和各失败原因的绝对计数。日志格式用于 `spec_patch_verify.sh` 脚本的 `ms_hit_rate` 指标提取。

---

## 3. MQA（多查询注意力）打分路径代码实现

### 3.1 概述

MQA 打分路径是投机解码验证阶段的一种优化。在标准批扩展（BatchExpansion）路径中，验证请求会将所有 k 个草案 token 拼接为一个序列进行前向传递；而 MQA 路径利用多查询注意力特性，将每个草案 token 拆分为独立请求分别打分，避免了批扩展带来的填充（padding）开销和长序列前向传递开销。

**路径选择**：MQA 路径由环境变量 `VLLM_ASCEND_SPEC_MQA_BACKENDS` 控制，当当前注意力后端名称在该列表中时启用 MQA 路径，否则回退到批扩展路径。

### 3.2 MQA Scorer 核心实现

**文件**：`vllm/vllm/spec_decode/mqa_scorer.py`
**位置**：`MQAScorer.score_proposals()` 方法，第 13–163 行

#### 3.2.1 请求构建（Lazy Materialization）

```python
def score_proposals(
    self,
    execute_model_req: ExecuteModelRequest,
    proposals: SpeculativeProposals,
) -> SpeculativeScores:
    target_seq_group_metadata_list = []
    target_seq_id_start = max(
        get_all_seq_ids(execute_model_req.seq_group_metadata_list)) + 1
    proposal_token_ids_tensor = proposals.proposal_token_ids
    all_proposal_lengths = proposals.proposal_lens.tolist()
    for i, seq_group_metadata in enumerate(
            execute_model_req.seq_group_metadata_list):
        proposal_len = all_proposal_lengths[i]
        if proposal_len == 0:
            # Keep prompt seqs untouched (keep computed_tokens for chunks).
            target_seq_group_metadata_list.append(seq_group_metadata)
            continue

        seq_data_dict = seq_group_metadata.seq_data
        assert len(seq_data_dict) == 1
        seq_id = next(iter(seq_data_dict.keys()))

        seq_data: SequenceData = seq_data_dict[seq_id]
        prompt_token_ids = seq_data.get_prompt_token_ids()
        output_token_ids = seq_data.get_output_token_ids()
        # Lazy materialization: avoid a full-batch tensor->Python list
        # conversion for proposal_token_ids in the hot path.
        proposal_token_ids = proposal_token_ids_tensor[i,
                                                       :proposal_len].tolist()
        new_output_token_ids = [*output_token_ids, *proposal_token_ids]
        # ... 构造 target_seq_group_metadata
```

**关键设计——Lazy Materialization**：代码注释明确指出，使用 `proposal_token_ids_tensor[i, :proposal_len].tolist()` 分片读取而非一次性将整个 `proposal_token_ids` tensor 转为 Python 列表，可以避免在推理热路径中引入不必要的数据搬运开销。

#### 3.2.2 批量打分与输出组织

```python
        target_sampler_output = self._scorer_worker.execute_model(
            execute_model_req=execute_model_req.clone(
                seq_group_metadata_list=target_seq_group_metadata_list))

        target_sampler_output = target_sampler_output[0]

        k = execute_model_req.num_lookahead_slots
        bs = len(execute_model_req.seq_group_metadata_list)
        target_token_ids = target_sampler_output.sampled_token_ids
        target_probs = target_sampler_output.sampled_token_probs
        target_logprobs = target_sampler_output.logprobs
```

#### 3.2.3 统一长度与混合长度分支

```python
        # If all requests have the same number of query tokens, we can avoid
        # the for loop to build output for better performance.
        if min(all_proposal_lengths) == k:
            # Regular decodes only.
            assert all(not sg.is_prompt
                       for sg in target_seq_group_metadata_list
                       if sg.is_prompt)
            bs, _ = proposals.proposal_token_ids.shape
            all_tokens = target_token_ids.reshape(bs, k + 1)
            all_probs = target_probs.reshape(bs, k + 1, self._vocab_size)
            all_logprobs = target_logprobs.reshape(bs, k + 1, self._vocab_size)
        else:
            # We either have decodes with different lens or prefill+decodes.
            # ... 混合长度分支，通过 split loop 分别处理 prefill 和 decode
```

**说明**：当所有请求的 proposal 长度一致（均为 k）时，使用批量 reshape 路径，避免循环开销；当存在混合长度或 prefill+decode 混合场景时，使用分叉循环分别处理两种情况。

#### 3.2.4 Hidden States 返回

```python
        hidden_states = None
        if target_sampler_output.hidden_states is not None:
            hidden_states = target_sampler_output.hidden_states.reshape(
                bs, (k + 1), -1)

        return SpeculativeScores(probs=all_probs,
                                 token_ids=all_tokens,
                                 logprobs=all_logprobs,
                                 hidden_states=hidden_states,
                                 prompt_logprobs=prompt_logprobs)
```

### 3.3 MQA 路径命中检测

MQA 路径的命中检测通过环境变量白名单实现，路径切换逻辑位于 `vllm-ascend` 对 `mqa_scorer.py` 的调用层。当 `VLLM_ASCEND_SPEC_MQA_BACKENDS` 环境变量包含当前后端名称时，投机解码验证阶段使用 `MQAScorer`；否则使用标准的 `BatchExpansionScorer`。相关代码通过 `spec_patch_verify.sh` 脚本中的 `mqa_mode` 字段输出当前打分路径类型。

---

## 4. Logits 对齐校准（Logits Alignment）代码实现

### 4.1 概述

Logits 对齐是针对草稿模型（Draft）与目标模型（Target）输出分布差异的校准机制。由于草稿模型（Qwen3-0.6B）与目标模型（Qwen3-8B）在参数规模和表达能力上存在差异，两者对同一 token 的 logit 分布可能存在系统性偏移。对齐校准通过在草稿模型的 softmax 之前对 logits 进行线性变换（温度缩放 + 仿射变换），使草稿侧的 token 概率分布更接近目标侧，从而提升接受率。

**文件**：`vllm-ascend/vllm_ascend/worker/draft_model_runner.py`
**位置**：`TP1DraftModelRunner.__init__` 配置段（第 52–68 行）及 `_align_logits()` 方法（第 304–327 行）

### 4.2 配置参数读取

```python
# Logits alignment controls used by model-alignment experiments.
self._align_enable = os.getenv("VLLM_ASCEND_DRAFT_ALIGN_ENABLE", "0") == "1"
self._align_mode = os.getenv("VLLM_ASCEND_DRAFT_ALIGN_MODE",
                              "temperature").strip().lower()
self._align_temperature = self._read_float_env(
    "VLLM_ASCEND_DRAFT_ALIGN_TEMPERATURE", 1.0, min_value=1e-6)
self._align_scale = self._read_float_env("VLLM_ASCEND_DRAFT_ALIGN_SCALE", 1.0)
self._align_bias = self._read_float_env("VLLM_ASCEND_DRAFT_ALIGN_BIAS", 0.0)
self._align_vocab_bias_path = os.getenv(
    "VLLM_ASCEND_DRAFT_ALIGN_VOCAB_BIAS_PATH", "").strip()
```

**对齐参数说明**：

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `VLLM_ASCEND_DRAFT_ALIGN_ENABLE` | `0` | 总开关，设为 `1` 启用对齐校准 |
| `VLLM_ASCEND_DRAFT_ALIGN_MODE` | `temperature` | 对齐模式：`temperature` / `legacy_affine` / `temperature_then_affine` |
| `VLLM_ASCEND_DRAFT_ALIGN_TEMPERATURE` | `1.0` | 温度系数 $T$，对 logits 做 $logits / T$ 的缩放（$T > 0$） |
| `VLLM_ASCEND_DRAFT_ALIGN_SCALE` | `1.0` | 仿射缩放系数 $\alpha$，$logits \leftarrow \alpha \cdot logits$ |
| `VLLM_ASCEND_DRAFT_ALIGN_BIAS` | `0.0` | 仿射偏置 $b$，$logits \leftarrow logits + b$（注意：对全词表同一 bias 对 softmax 分布影响有限） |
| `VLLM_ASCEND_DRAFT_ALIGN_VOCAB_BIAS_PATH` | 空 | 可选词表 bias 向量路径（`.pt` 文件），为每个 token 独立添加 bias |

### 4.3 Logits 对齐实现（线性变换）

```python
def _align_logits(self, logits: torch.Tensor) -> torch.Tensor:
    if not self._align_enable:
        return logits

    mode = self._align_mode
    if mode == "temperature":
        logits = logits / self._align_temperature
        logits = logits * self._align_scale + self._align_bias
    elif mode == "legacy_affine":
        logits = logits * self._align_scale + self._align_bias
    elif mode == "temperature_then_affine":
        logits = logits / self._align_temperature
        logits = logits * self._align_scale + self._align_bias

    vocab_bias = self._get_vocab_bias_for_logits(logits)
    if vocab_bias is not None:
        logits = logits + vocab_bias

    return logits
```

**线性变换数学形式**：

- **温度模式（temperature）**：
  $$logits' = \frac{logits}{T} \cdot \alpha + b$$
  其中 $T$ 为温度系数，$\alpha$ 为缩放系数，$b$ 为偏置。

- **仿射模式（legacy_affine）**：
  $$logits' = \alpha \cdot logits + b$$

- **温度+仿射模式（temperature_then_affine）**：先温度缩放，再仿射变换，数学上等价于温度模式的单次运算，但保留了两阶段语义。

- **词表级 Bias（vocab_bias）**：通过外部 `.pt` 文件加载的词表级 bias 向量（形状为 `[V]`），为每个词表 token 独立添加 bias，理论上对 softmax 分布有实质影响。

**调用位置**：`execute_model()` 方法第 627 行：
```python
logits = self._align_logits(logits)
```
对齐操作在 `compute_logits()` 之后、`sampler()` 之前执行，符合"对齐应在 softmax 之前应用"的设计原则。

### 4.4 对齐配置日志

```python
def _maybe_log_align_config(self) -> None:
    if self._align_config_logged:
        return
    self._align_config_logged = True
    logger.info(
        "DRAFT_ALIGN config: enable=%s mode=%s temperature=%.4f scale=%.4f "
        "bias=%.4f vocab_bias_path=%s supports_previous_hidden_states=%s",
        self._align_enable,
        self._align_mode,
        self._align_temperature,
        self._align_scale,
        self._align_bias,
        self._align_vocab_bias_path or "<none>",
        self._model_supports_previous_hidden_states(),
    )
```

---

## 5. 动态步长控制器（Adaptive-K）代码实现

### 5.1 概述

动态步长控制器（Adaptive-K）根据运行时接受率等指标动态调整每轮的 proposal 步长 $k$，以在高负载下降低浪费率、在低负载下提升吞吐量。控制器分为两个级别：

- **adaptive（自适应控制器）**：基于浪费率（waste ratio）和第二位置接受率（pos2 acceptance）门控切换 $k$，使用 cooldown 机制防止频繁振荡。
- **adaptive_pro（增强型自适应控制器）**：在 adaptive 基础上增加 EWMA（指数加权移动平均）平滑统计和效用（utility）估计，选择期望 emit 率最高的 $k$ 值。

> **重要说明**：EWMA 统计仅用于 adaptive_pro（增强型控制器）。在 adaptive 模式（仅基于 waste ratio 切换）下，EWMA 默认关闭以避免在推理热路径中引入额外的 device→host 数据拷贝开销。

### 5.2 控制器参数初始化

**文件**：`vllm/vllm/spec_decode/spec_decode_worker.py`
**位置**：`SpecDecodeWorker.__init__` 中的自适应控制初始化段

```python
# --- Adaptive-K controller (off by default) ---
self.adaptive_k_enabled = os.getenv("VLLM_ASCEND_ADAPTIVE_K_ENABLE", "0") == "1"
self.adaptive_k_init = int(os.getenv("VLLM_ASCEND_ADAPTIVE_K_INIT", "0"))  # 0=use request k
self.adaptive_k_min = int(os.getenv("VLLM_ASCEND_ADAPTIVE_K_MIN", "2"))
self.adaptive_k_max = int(os.getenv("VLLM_ASCEND_ADAPTIVE_K_MAX", "4"))
if self.adaptive_k_min > self.adaptive_k_max:
    self.adaptive_k_min, self.adaptive_k_max = self.adaptive_k_max, self.adaptive_k_min

self.adaptive_k_current: Optional[int] = None
self.adaptive_cooldown_steps = int(os.getenv("VLLM_ASCEND_ADAPTIVE_COOLDOWN_STEPS", "8"))
self._adaptive_total_steps = 0
self._adaptive_last_update_step = 0

# Thresholds (from your k=2/4/8 data)
self.adaptive_waste_high = float(os.getenv("VLLM_ASCEND_ADAPTIVE_WASTE_HIGH", "0.34"))
self.adaptive_waste_mid = float(os.getenv("VLLM_ASCEND_ADAPTIVE_WASTE_MID", "0.25"))
self.adaptive_waste_low = float(os.getenv("VLLM_ASCEND_ADAPTIVE_WASTE_LOW", "0.15"))
self.adaptive_p2_high = float(os.getenv("VLLM_ASCEND_ADAPTIVE_P2_HIGH", "0.75"))
self.adaptive_p3_low = float(os.getenv("VLLM_ASCEND_ADAPTIVE_P3_LOW", "0.55"))
self.adaptive_p4_low = float(os.getenv("VLLM_ASCEND_ADAPTIVE_P4_LOW", "0.43"))
self.adaptive_queue_up = int(os.getenv("VLLM_ASCEND_ADAPTIVE_QUEUE_UP", "64"))
```

**关键参数说明**：

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `VLLM_ASCEND_ADAPTIVE_K_ENABLE` | `0` | 自适应控制器总开关 |
| `VLLM_ASCEND_ADAPTIVE_K_INIT` | `0` | 初始 $k$（0 表示使用请求指定的 $k$） |
| `VLLM_ASCEND_ADAPTIVE_K_MIN` | `2` | 自适应 $k$ 下限 |
| `VLLM_ASCEND_ADAPTIVE_K_MAX` | `4` | 自适应 $k$ 上限 |
| `VLLM_ASCEND_ADAPTIVE_COOLDOWN_STEPS` | `8` | 两次 $k$ 切换之间的最小步数间隔 |

### 5.3 EWMA 统计与 Utility 增强参数（仅 adaptive_pro）

```python
# ---- utility-based adaptive-k state ----
self.adaptive_enable_utility = os.getenv("VLLM_ASCEND_ADAPTIVE_ENABLE_UTILITY", "0") == "1"
# EWMA stats are only needed by utility-based adaptive-k. Keeping this
# off by default avoids per-step device->host copies in the hot path.
self._adaptive_ewma_enable = (
    self.adaptive_enable_utility
    or os.getenv("VLLM_ASCEND_ADAPTIVE_EWMA_ENABLE", "0") == "1")
self.adaptive_ewma_beta = float(os.getenv("VLLM_ASCEND_ADAPTIVE_EWMA_BETA", "0.2"))
self.adaptive_switch_margin = float(os.getenv("VLLM_ASCEND_ADAPTIVE_SWITCH_MARGIN", "0.03"))
self.adaptive_min_dwell_steps = int(os.getenv("VLLM_ASCEND_ADAPTIVE_MIN_DWELL_STEPS", "12"))

self._k_step_accept_ewma: Dict[int, torch.Tensor] = {}
self._k_stage_time_ewma: Dict[int, torch.Tensor] = {}
self._adaptive_last_switch_step = 0
```

**说明**：代码注释明确指出——**EWMA 统计仅服务于 utility-based adaptive-k（adaptive_pro）**。默认关闭 EWMA 以避免在推理热路径中引入不必要的 device→host 数据拷贝开销。只有当 `VLLM_ASCEND_ADAPTIVE_ENABLE_UTILITY=1`（adaptive_pro 模式）或显式设置 `VLLM_ASCEND_ADAPTIVE_EWMA_ENABLE=1` 时，EWMA 才会被启用。

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `VLLM_ASCEND_ADAPTIVE_ENABLE_UTILITY` | `0` | **adaptive_pro 增强开关**，设为 `1` 启用 EWMA 效用估计 |
| `VLLM_ASCEND_ADAPTIVE_EWMA_BETA` | `0.2` | EWMA 平滑系数 |
| `VLLM_ASCEND_ADAPTIVE_SWITCH_MARGIN` | `0.03` | 效用切换 margin，效用需超过当前最优值的 $(1 + margin)$ 倍才触发切换 |
| `VLLM_ASCEND_ADAPTIVE_MIN_DWELL_STEPS` | `12` | 最小停留步数，防止频繁切换 |

### 5.4 对齐感知 K 上限（Adaptive Align Gate）

```python
# Optional phase-4: explicit alignment-aware gating for adaptive-k.
self.adaptive_align_gate_enable = (
    os.getenv("VLLM_ASCEND_ADAPTIVE_ALIGN_GATE_ENABLE", "0") == "1")
self.adaptive_align_low_th = float(
    os.getenv("VLLM_ASCEND_ADAPTIVE_ALIGN_LOW_TH", "0.45"))
self.adaptive_align_high_th = float(
    os.getenv("VLLM_ASCEND_ADAPTIVE_ALIGN_HIGH_TH", "0.70"))
self.adaptive_align_cap_low = int(
    os.getenv("VLLM_ASCEND_ADAPTIVE_ALIGN_CAP_LOW", "3"))
self.adaptive_align_cap_high = int(
    os.getenv("VLLM_ASCEND_ADAPTIVE_ALIGN_CAP_HIGH", "8"))
self.adaptive_align_w_pos2 = float(
    os.getenv("VLLM_ASCEND_ADAPTIVE_ALIGN_W_POS2", "0.45"))
self.adaptive_align_w_nowaste = float(
    os.getenv("VLLM_ASCEND_ADAPTIVE_ALIGN_W_NOWASTE", "0.45"))
self.adaptive_align_w_delta_hs = float(
    os.getenv("VLLM_ASCEND_ADAPTIVE_ALIGN_W_DELTA_HS", "0.10"))

self._adaptive_align_score_ewma: Optional[float] = None
self._adaptive_delta_hs_ewma: Optional[float] = None
self._adaptive_align_runtime_k_cap: Optional[int] = None
```

**说明**：对齐门控（Adaptive Align Gate）在 `EXPERIMENT_MODE=joint` 时自动启用，通过 EWMA 平滑的综合得分（pos2 接受率 + 无浪费率 + 隐藏状态变化）对 $k$ 的运行时上限进行动态约束。综合得分的权重通过 `w_pos2`、`w_nowaste`、`w_delta_hs` 三个环境变量配置。

### 5.5 K 值选择入口

**文件**：`vllm/vllm/spec_decode/spec_decode_worker.py`
**位置**：第 604–624 行

```python
# Adaptive-K chooses k for this decode step
scheduled_lookahead_slots = num_lookahead_slots

if (self.adaptive_k_enabled and not all_prompt and not disable_all_speculation
        and not all_zero_spec_tokens and num_lookahead_slots > 0):
    if self.adaptive_k_current is None:
        init_k = self.adaptive_k_init if self.adaptive_k_init > 0 else num_lookahead_slots
        self.adaptive_k_current = max(self.adaptive_k_min, min(self.adaptive_k_max, init_k))

    effective_k_max = min(self.adaptive_k_max, max(1, scheduled_lookahead_slots))
    effective_k_min = min(self.adaptive_k_min, effective_k_max)
    self._adaptive_runtime_k_cap = effective_k_max

    self.adaptive_k_current = max(
        effective_k_min,
        min(effective_k_max, self.adaptive_k_current),
    )

    num_lookahead_slots = self.adaptive_k_current
    execute_model_req.num_lookahead_slots = num_lookahead_slots
```

**说明**：在每次投机解码步骤开始时，根据调度器分配的 `lookahead_slots` 和当前自适应 $k$ 值，取两者交集作为本步的有效 $k$，并更新 `execute_model_req.num_lookahead_slots`。

### 5.6 EWMA 效用估计（仅 adaptive_pro）

```python
def _update_k_ewma(self, k: int, step_accept: torch.Tensor,
               stage_times: Tuple[float, float, float]) -> None:
    beta = self.adaptive_ewma_beta
    s = step_accept.detach().float().cpu()
    t = torch.tensor(stage_times, dtype=torch.float32)  # [proposal_per_tok, scoring, verify]

    if k not in self._k_step_accept_ewma:
        self._k_step_accept_ewma[k] = s.clone()
    else:
        old = self._k_step_accept_ewma[k]
        if old.numel() != s.numel():
            old = torch.nn.functional.pad(old, (0, max(0, s.numel() - old.numel())))[:s.numel()]
        self._k_step_accept_ewma[k] = (1 - beta) * old + beta * s

    if k not in self._k_stage_time_ewma:
        self._k_stage_time_ewma[k] = t.clone()
    else:
        self._k_stage_time_ewma[k] = (1 - beta) * self._k_stage_time_ewma[k] + beta * t

def _estimate_utility(self, k: int) -> Optional[float]:
    if k not in self._k_step_accept_ewma or k not in self._k_stage_time_ewma:
        return None
    a = self._k_step_accept_ewma[k]
    t = self._k_stage_time_ewma[k]
    expected_emit = self._estimate_expected_emitted(a)

    # 效用 = 期望 emit token 数 / 总耗时
    total_ms = t.sum().item()
    if total_ms <= 0:
        return None
    return expected_emit / total_ms

def _select_k_by_utility(self, current_k: int, k_min: int, k_max: int) -> int:
    if not self.adaptive_enable_utility:  # 仅 adaptive_pro 调用
        return current_k

    if self._adaptive_total_steps - self._adaptive_last_switch_step < self.adaptive_min_dwell_steps:
        return current_k

    best_k = current_k
    best_u = self._estimate_utility(current_k) or -1.0

    for cand_k in range(k_min, k_max + 1):
        u = self._estimate_utility(cand_k)
        if u is None:
            continue
        # 效用需超过当前最优值的 margin 倍才触发切换
        if u > best_u * (1.0 + self.adaptive_switch_margin):
            best_k, best_u = cand_k, u

    if best_k != current_k:
        self._adaptive_last_switch_step = self._adaptive_total_steps
        self._adaptive_switch_count += 1
    return best_k
```

**说明**：`_select_k_by_utility()` 仅在 `adaptive_enable_utility=True`（即 adaptive_pro 模式）时执行实际切换逻辑；在 adaptive 模式下，该方法直接返回当前 $k$，不做效用估计。

### 5.7 自适应 K 更新逻辑

```python
def _maybe_update_adaptive_k(self, queue_depth: int, step_accept: torch.Tensor,
                            waste_ratio: float) -> None:
    """Update adaptive k with safety cap from scheduler-provided lookahead."""
    if not self.adaptive_k_enabled or self.adaptive_k_current is None:
        return

    self._adaptive_total_steps += 1
    if (self._adaptive_total_steps - self._adaptive_last_update_step
            < self.adaptive_cooldown_steps):
        return

    # ... (waste ratio 门控切换逻辑)

    # adaptive_pro: 效用估计选择 k
    new_k = self._select_k_by_utility(self.adaptive_k_current, k_min, k_max)
    if new_k != self.adaptive_k_current:
        self._adaptive_k_history.append(new_k)
        logger.info(
            "AdaptiveK k_change from %d to %d (align_cap=%s)",
            self.adaptive_k_current, new_k, str(runtime_cap))
        self.adaptive_k_current = new_k
```

---

## 6. 各模块与实验配置对照表

| 实验脚本 | FastPath | MQA | Logits Align | Adaptive-K | EWMA |
|----------|:--------:|:---:|:------------:|:----------:|:----:|
| `model_alignment_bench.sh` A0 | 继承默认 | 继承默认 | 关闭 | 关闭 | N/A |
| `model_alignment_bench.sh` A2 | 继承默认 | 继承默认 | **开启** | 关闭 | N/A |
| `model_alignment_bench_v2.sh` adaptive | 继承默认 | 继承默认 | 关闭 | **开启** | **关闭** |
| `model_alignment_bench_v2.sh` adaptive_pro | 继承默认 | 继承默认 | 关闭 | **开启** | **开启** |
| `model_alignment_bench_v2.sh` joint | 继承默认 | 继承默认 | **开启** | **开启** | **开启** |
| `spec_patch_verify.sh` A组 | **开启** | **开启** | 继承默认 | 关闭 | 关闭 |
| `spec_patch_verify.sh` B组 | **开启** | 关闭 | 继承默认 | 关闭 | 关闭 |
| `spec_patch_verify.sh` C组 | 关闭 | **开启** | 继承默认 | 关闭 | 关闭 |
| `spec_patch_verify.sh` D组 | 关闭 | 关闭 | 继承默认 | 关闭 | 关闭 |

> **注**：
> - "继承默认"表示该参数以环境变量默认值运行，未被实验脚本显式修改。
> - EWMA 仅在 adaptive_pro 模式（`ADAPTIVE_ENABLE_UTILITY=1`）下启用；在 adaptive 模式（`VLLM_ASCEND_ADAPTIVE_K_ENABLE=1` 但 `ADAPTIVE_ENABLE_UTILITY=0`）下，EWMA 统计被关闭以避免热路径开销。

---

## 7. 关键代码文件索引

| 功能 | 文件路径 | 核心方法/类 |
|------|----------|-------------|
| FastPath 能力检测与统计 | `vllm-ascend/vllm_ascend/worker/draft_model_runner.py` | `supports_gpu_multi_step()`, `_log_gpu_multi_step_stats()` |
| FastPath 后端支持检测 | `vllm-ascend/vllm_ascend/worker/draft_model_runner.py` | `_attn_backend_supports_gpu_multi_step()` |
| Logits 对齐校准 | `vllm-ascend/vllm_ascend/worker/draft_model_runner.py` | `_align_logits()`, `_maybe_log_align_config()` |
| MQA 打分路径 | `vllm/vllm/spec_decode/mqa_scorer.py` | `MQAScorer.score_proposals()` |
| 自适应 K 控制器 | `vllm/vllm/spec_decode/spec_decode_worker.py` | `_maybe_update_adaptive_k()`, `_select_k_by_utility()` |
| EWMA 统计更新 | `vllm/vllm/spec_decode/spec_decode_worker.py` | `_update_k_ewma()`, `_estimate_utility()` |
| 对齐感知 K 上限 | `vllm/vllm/spec_decode/spec_decode_worker.py` | `_maybe_apply_align_aware_cap()` |

---

*本文档对应毕设论文终稿（v1.0），所有代码基于 vLLM-Ascend v0.9.1（`VLLM_USE_V1=0`）编写。*
